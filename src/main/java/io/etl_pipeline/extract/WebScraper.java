package io.etl_pipeline.extract;

import org.jsoup.Jsoup;
import org.jsoup.nodes.Document;

import java.io.IOException;

/**
 * Establishes connection to the yad2.co.il website.
 * Parses the HTML to extract the required data using a library like JSoup.
 * Handles HTTP requests and error handling.
 * Returns the data in a structured format (e.g., a list of objects).
 */
public class WebScraper {
    public void scrape(String url) {

    }




}
